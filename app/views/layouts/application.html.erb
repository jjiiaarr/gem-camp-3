<!DOCTYPE html>
<html>
  <head>
    <title>Welcome to Gem Camp 3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/ff982a8a24.js" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload"%>
    <%= stylesheet_link_tag "style.css"%>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
    <%= render "layouts/footer" %>
    <%= t( "hello", name: current_user.email) if current_user %>
    <%= link_to "EN", params.permit!.merge(locale: 'en') %>
    <%= link_to "zh-CN", params.permit!.merge(locale: 'zh-CN') %>
    <%= yield %>
  </body>
</html>
